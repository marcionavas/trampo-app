{
  "name": "users",
  "version": "1.0.0",
  "description": "Microserviço de usuários do Trampo APP",
  "main": "dist/main.js",
  "type": "commonjs",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:dev:nodemon": "nodemon --config nodemon.json",
    "build": "nest build",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "nx:build": "npx nx build users",
    "nx:serve": "npx nx serve users",
    "test:e2e": "jest --config ./test/jest-e2e.json"
  }
}